export class Livro {
    codigo: number = 0; // Inicialize com um valor padrão
    codEditora: number = 0; // Inicialize com um valor padrão
    titulo: string = ''; // Inicialize com um valor padrão
    resumo: string = ''; // Inicialize com um valor padrão
    autores: string[] = []; // Inicialize com um valor padrão
  }